package net.aurxenon.ThistleMapEditor.Utils;

import java.util.Random;

public class GameMap {

    public GameMap(){}

    public void generateMap() {
        /*Random rnd = new Random();
        OpenSimplexNoise noiseCreator = new OpenSimplexNoise(rnd.nextInt(100));
        double noiseValue = noiseCreator.eval(x / 24.0,y / 24.0);
        if (noiseValue <= -0.2 || noiseValue >= 0.2){
            terminal.applyBackgroundColor(0, 255, 0);
            terminal.applyForegroundColor(0, 255, 0);
            terminal.putCharacter('#');
        }
        if (noiseValue > -0.2 && noiseValue < 0.2){
            terminal.applyBackgroundColor(0, 0, 255);
            terminal.applyForegroundColor(0, 0, 255);
            terminal.putCharacter('#');
        }*/
    }
}